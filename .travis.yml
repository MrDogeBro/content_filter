language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"      # current default Python on Travis CI
  - "3.7"
  - "3.8"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - nosetests tests.filter_test
  - nosetests tests.custom_filter_test.py

notifications:
  email:
    on_success: never
    on_failure: always